import React from "react";
import { Header } from "../../Components";

interface Props {}

const Graduates = (props: Props) => {
  return <Header attribute={"unclear"} />;
};

export default Graduates;
